# Phase 1a: Core Validation Experiment
# Reference: docs/phase1a-experiment.md

# Core dependencies
torch>=2.0
transformers>=4.40
datasets>=2.14
tqdm>=4.65
accelerate>=1.12

# Analysis and visualization
pandas>=2.0
matplotlib>=3.7

# Optional: for flash attention (recommended for faster inference)
# flash-attn>=2.0

# Optional: for quantized models
# bitsandbytes>=0.41

# Optional: experiment tracking (enabled by default if installed)
# pip install git+https://github.com/urverwirrung/experiment-tracker.git
# Tracking uses /data/experiments/experiments.db by default
# Override with EXPERIMENTS_DB or EXPERIMENTS_ROOT env vars

# Optional: distributed execution with artifact management
# pip install -e ~/repos/experiment-runner
# Use with: python run_phase1a.py --distributed "..." --use-experiment-runner
